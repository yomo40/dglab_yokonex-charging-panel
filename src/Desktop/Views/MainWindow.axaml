<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="ChargingPanel.Desktop.Views.MainWindow"
        Title="郊狼&amp;役次元游戏适配面板"
        Width="1200" Height="800"
        MinWidth="1000" MinHeight="700"
        WindowStartupLocation="CenterScreen"
        Background="#11111B">
    
    <Window.Styles>
        <!-- 导航按钮基础样式 -->
        <Style Selector="Button.nav">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#6C7086" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding" Value="16,13" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Margin" Value="0,2" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Transitions">
                <Transitions>
                    <BrushTransition Property="Background" Duration="0:0:0.2" />
                    <BrushTransition Property="Foreground" Duration="0:0:0.2" />
                    <TransformOperationsTransition Property="RenderTransform" Duration="0:0:0.15" />
                </Transitions>
            </Setter>
        </Style>
        <Style Selector="Button.nav:pointerover">
            <Setter Property="Foreground" Value="#A6ADC8" />
            <Setter Property="RenderTransform" Value="translateX(4px)" />
        </Style>
        <Style Selector="Button.nav:pointerover /template/ ContentPresenter">
            <Setter Property="Background" Value="#1E1E2E" />
        </Style>
        
        <!-- 导航按钮激活样式 -->
        <Style Selector="Button.nav-active">
            <Setter Property="Background" Value="#313244" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding" Value="16,13" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Margin" Value="0,2" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderBrush" Value="#3B82F6" />
            <Setter Property="BorderThickness" Value="0,0,0,0" />
        </Style>
        <Style Selector="Button.nav-active /template/ ContentPresenter">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,0%">
                        <GradientStop Offset="0" Color="#3B82F620" />
                        <GradientStop Offset="1" Color="#31324480" />
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Styles>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <!-- 侧边栏 - 增强视觉效果 -->
        <Border Grid.Column="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0%,0%" EndPoint="0%,100%">
                    <GradientStop Offset="0" Color="#181825" />
                    <GradientStop Offset="1" Color="#11111B" />
                </LinearGradientBrush>
            </Border.Background>
            <DockPanel Margin="14">
                <!-- Logo -->
                <Border DockPanel.Dock="Top" Margin="0,12,0,28">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="⚡" FontSize="42" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                        <TextBlock Text="游戏适配面板" FontSize="17" FontWeight="Bold"
                                   Foreground="White" HorizontalAlignment="Center" Margin="0,10,0,0" />
                        <Border Background="#3B82F620" CornerRadius="8" Padding="10,4" Margin="0,6,0,0"
                                HorizontalAlignment="Center">
                            <TextBlock Text="v0.9.7" FontSize="11" Foreground="#3B82F6" FontWeight="SemiBold" />
                        </Border>
                    </StackPanel>
                </Border>

                <!-- 导航菜单 - 优化分组样式 -->
                <ScrollViewer DockPanel.Dock="Top" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Border Background="#1E1E2E40" CornerRadius="8" Padding="10,6" Margin="0,0,0,10">
                            <TextBlock Text="主要功能" FontSize="11" Foreground="#6C7086" 
                                       FontWeight="Bold" LetterSpacing="1" />
                        </Border>
                        
                        <Button x:Name="NavOverview" Content="🏠  概览" Classes="nav-active"
                                Click="OnNavClick" Tag="Overview" />
                        <Button x:Name="NavDevices" Content="📱  设备管理" Classes="nav"
                                Click="OnNavClick" Tag="Devices" />
                        <Button x:Name="NavControl" Content="🎚  设备控制" Classes="nav"
                                Click="OnNavClick" Tag="Control" />
                        
                        <Border Background="#1E1E2E40" CornerRadius="8" Padding="10,6" Margin="0,20,0,10">
                            <TextBlock Text="游戏联动" FontSize="11" Foreground="#6C7086" 
                                       FontWeight="Bold" LetterSpacing="1" />
                        </Border>
                        
                        <Button x:Name="NavOCR" Content="👁  血量识别" Classes="nav"
                                Click="OnNavClick" Tag="OCR" />
                        <Button x:Name="NavEvents" Content="⚡  电击规则" Classes="nav"
                                Click="OnNavClick" Tag="Events" />
                        <Button x:Name="NavSensor" Content="📡  传感器" Classes="nav"
                                Click="OnNavClick" Tag="Sensor" />
                        <Button x:Name="NavPlugins" Content="🎮  游戏适配" Classes="nav"
                                Click="OnNavClick" Tag="Plugins" />
                        <Button x:Name="NavRoom" Content="👥  多人房间" Classes="nav"
                                Click="OnNavClick" Tag="Room" />
                        
                        <Border Background="#1E1E2E40" CornerRadius="8" Padding="10,6" Margin="0,20,0,10">
                            <TextBlock Text="系统" FontSize="11" Foreground="#6C7086" 
                                       FontWeight="Bold" LetterSpacing="1" />
                        </Border>
                        
                        <Button x:Name="NavLogs" Content="📋  日志" Classes="nav"
                                Click="OnNavClick" Tag="Logs" />
                        <Button x:Name="NavSettings" Content="⚙  系统设置" Classes="nav"
                                Click="OnNavClick" Tag="Settings" />
                    </StackPanel>
                </ScrollViewer>
                
                <!-- 底部状态 - 增强紧急停止按钮 -->
                <Border DockPanel.Dock="Bottom" CornerRadius="14" Padding="14" Margin="0,16,0,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,100%">
                            <GradientStop Offset="0" Color="#1E1E2E" />
                            <GradientStop Offset="1" Color="#252538" />
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel Spacing="12">
                        <Button HorizontalAlignment="Stretch" Padding="0,12" CornerRadius="10"
                                FontWeight="Bold" FontSize="13" Click="OnEmergencyStopClick">
                            <Button.Background>
                                <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,0%">
                                    <GradientStop Offset="0" Color="#DC2626" />
                                    <GradientStop Offset="1" Color="#EF4444" />
                                </LinearGradientBrush>
                            </Button.Background>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8">
                                <TextBlock Text="🛑" FontSize="16" VerticalAlignment="Center" />
                                <TextBlock Text="紧急停止" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="4">
                            <TextBlock Text="Made with" Foreground="#45475A" FontSize="10" />
                            <TextBlock Text="❤️" FontSize="10" />
                            <TextBlock Text="by yomo40" Foreground="#45475A" FontSize="10" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </DockPanel>
        </Border>

        <!-- 主内容区 - 增强视觉层次 -->
        <Border Grid.Column="1" CornerRadius="28,0,0,0" Margin="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,100%">
                    <GradientStop Offset="0" Color="#1E1E2E" />
                    <GradientStop Offset="1" Color="#1a1a28" />
                </LinearGradientBrush>
            </Border.Background>
            <Grid Margin="32,28">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <!-- 页面标题栏 - 增强样式 -->
                <Grid Grid.Row="0" Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="PageTitle" Text="概览" FontSize="28" 
                                   FontWeight="Bold" Foreground="White" />
                        <TextBlock x:Name="PageDescription" Text="设备状态总览与快捷控制" 
                                   FontSize="14" Foreground="#6C7086" Margin="0,8,0,0" />
                    </StackPanel>
                    
                    <!-- 全局保存按钮 (仅在设置页面可见) -->
                    <Button x:Name="GlobalSaveButton" Grid.Column="1" 
                            Padding="18,12" CornerRadius="12"
                            FontWeight="Bold" FontSize="13"
                            VerticalAlignment="Center"
                            Margin="0,0,14,0"
                            IsVisible="False"
                            Click="OnGlobalSaveClick">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,0%">
                                <GradientStop Offset="0" Color="#10B981" />
                                <GradientStop Offset="1" Color="#34d399" />
                            </LinearGradientBrush>
                        </Button.Background>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="💾" FontSize="14" VerticalAlignment="Center" />
                            <TextBlock Text="保存设置" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    
                    <!-- 状态通知区域 (右上角) - 增强样式 -->
                    <Border Grid.Column="2" CornerRadius="12" 
                            Padding="14,10" MinWidth="200" VerticalAlignment="Center"
                            BorderBrush="#31324480" BorderThickness="1">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,0%">
                                <GradientStop Offset="0" Color="#252538" />
                                <GradientStop Offset="1" Color="#2a2a40" />
                            </LinearGradientBrush>
                        </Border.Background>
                        <Grid ColumnDefinitions="Auto,*">
                            <Border CornerRadius="6" Width="12" Height="12" Margin="0,0,12,0"
                                    VerticalAlignment="Center" Background="#6B7280">
                                <Ellipse x:Name="TopStatusIndicator" Width="12" Height="12" Fill="#6B7280" />
                            </Border>
                            <TextBlock x:Name="TopStatusText" Grid.Column="1"
                                       Text="未连接设备" 
                                       Foreground="#CDD6F4" FontSize="13" FontWeight="Medium"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"
                                       MaxWidth="220" />
                        </Grid>
                    </Border>
                </Grid>
                
                <!-- 页面容器 -->
                <Border Grid.Row="1" x:Name="PageContainer" />
            </Grid>
        </Border>
    </Grid>
</Window>
